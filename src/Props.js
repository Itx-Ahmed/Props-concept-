import React from 'react'

const Props = (props) => {
  return (
    <>
    <div>{props.name}</div>
    <div>{props.age}</div>
    <div>{props.Education}</div>
    <div>{props.replacement}</div>
    </>
  )
}

export default Props